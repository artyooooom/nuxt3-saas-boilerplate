// _*: define the application's meta info & support chat embed code (if got one)
export default defineAppConfig({
    appName: 'NuxtSAAS Application',
    appLogo: null,
    supportChat: {
        isEnabled: false,
        embed: ''
    }
});